# ============================================================
# requirements_m3.txt
# MedOracle — Member 3 (Adshaya): Explainability + Web App
# Install: pip install -r requirements_shared.txt -r requirements_m3.txt
# ============================================================

# SHAP
shap>=0.43.0

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-jose[cryptography]>=3.3.0   # JWT auth
passlib[bcrypt]>=1.7.4             # Password hashing
pydantic>=2.4.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.7             # PostgreSQL driver (prod)
aiosqlite>=0.19.0                  # SQLite async driver (dev)
alembic>=1.12.0                    # DB migrations

# LLM API client
anthropic>=0.20.0                  # Claude API
# openai>=1.3.0                    # GPT-4 alternative (uncomment if needed)

# HTTP client
httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
