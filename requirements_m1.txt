# ============================================================
# requirements_m1.txt
# MedOracle — Member 1 (Suhira): Physiological Module
# Install: pip install -r requirements_shared.txt -r requirements_m1.txt
# ============================================================

# EEG / GSR processing
mne>=1.6.0                  # EEG preprocessing, ICA, filtering
pyedflib>=0.1.22            # Read DEAP .bdf files

# Deep learning
torch>=2.1.0
torchvision>=0.16.0

# ML utilities
scikit-learn>=1.3.0         # compute_class_weight, metrics, GroupKFold

# Data
pandas>=2.0.0
h5py>=3.9.0                 # DEAP .mat files (v7.3 HDF5 format)
mat73>=0.60                 # legacy .mat file fallback

# Testing
pytest>=7.4.0
